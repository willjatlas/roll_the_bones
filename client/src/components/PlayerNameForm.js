const PlayerNameForm = ({handleNameFormButton})=>{
    return(
        <>
            <h5>This is the name form</h5>
            <button onClick={handleNameFormButton}>Close</button>
        </>
    );
};

export default PlayerNameForm;